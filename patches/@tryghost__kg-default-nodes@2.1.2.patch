diff --git a/build/cjs/nodes/callout/CalloutNode.js b/build/cjs/nodes/callout/CalloutNode.js
index 8fbb3f7b33a069c5bb0ab12e1f51cf940f6c5656..d2b232fbf8748341cacd422e7809164d31896bcd 100644
--- a/build/cjs/nodes/callout/CalloutNode.js
+++ b/build/cjs/nodes/callout/CalloutNode.js
@@ -5,10 +5,43 @@ exports.$isCalloutNode = $isCalloutNode;
 const generate_decorator_node_js_1 = require("../../generate-decorator-node.js");
 const callout_renderer_js_1 = require("./callout-renderer.js");
 const callout_parser_js_1 = require("./callout-parser.js");
+const legacyColorToType = {
+    white: 'note',
+    grey: 'note',
+    blue: 'info',
+    green: 'tip',
+    yellow: 'warning',
+    red: 'danger',
+    pink: 'danger',
+    purple: 'important',
+    accent: 'important'
+};
+const typeDefaultIcons = {
+    note: 'info',
+    info: 'info',
+    tip: 'lightbulb',
+    hint: 'lightbulb',
+    important: 'sparkles',
+    warning: 'triangle-alert',
+    caution: 'triangle-alert',
+    attention: 'triangle-alert',
+    danger: 'circle-x',
+    error: 'circle-x',
+    bug: 'circle-x',
+    quote: 'message-square-quote',
+    cite: 'message-square-quote'
+};
+const validTypes = new Set([...Object.keys(typeDefaultIcons)]);
+function normalizeCalloutType(type) {
+    const normalizedType = (type || '').toLowerCase();
+    return validTypes.has(normalizedType) ? normalizedType : legacyColorToType[normalizedType] || 'info';
+}
 const calloutProperties = [
     { name: 'calloutText', default: '', wordCount: true },
-    { name: 'calloutEmoji', default: '💡' },
-    { name: 'backgroundColor', default: 'blue' }
+    { name: 'calloutTitle', default: '' },
+    { name: 'calloutIcon', default: 'info' },
+    { name: 'calloutEmoji', default: '' },
+    { name: 'backgroundColor', default: 'info' }
 ];
 class CalloutNode extends (0, generate_decorator_node_js_1.generateDecoratorNode)({
     nodeType: 'callout',
@@ -16,11 +49,14 @@ class CalloutNode extends (0, generate_decorator_node_js_1.generateDecoratorNode
     defaultRenderFn: callout_renderer_js_1.renderCalloutNode
 }) {
     /* override */
-    constructor({ calloutText, calloutEmoji, backgroundColor } = {}, key) {
+    constructor({ calloutText, calloutTitle, calloutIcon, calloutEmoji, backgroundColor } = {}, key) {
         super({}, key);
+        const type = normalizeCalloutType(backgroundColor);
         this.__calloutText = calloutText || '';
-        this.__calloutEmoji = calloutEmoji ?? '💡';
-        this.__backgroundColor = backgroundColor || 'blue';
+        this.__calloutTitle = calloutTitle || '';
+        this.__calloutIcon = calloutIcon || typeDefaultIcons[type] || 'info';
+        this.__calloutEmoji = calloutEmoji || '';
+        this.__backgroundColor = type;
     }
     static importDOM() {
         return (0, callout_parser_js_1.parseCalloutNode)(this);
@@ -34,3 +70,4 @@ const $createCalloutNode = (dataset = {}) => {
     return new CalloutNode(dataset);
 };
 exports.$createCalloutNode = $createCalloutNode;
+//# sourceMappingURL=CalloutNode.js.map
diff --git a/build/cjs/nodes/callout/callout-parser.js b/build/cjs/nodes/callout/callout-parser.js
index 8474942cd18ff002c014e9507d364f601033a070..9c891253ea6675622af1c7c3d1a4716b9479df3f 100644
--- a/build/cjs/nodes/callout/callout-parser.js
+++ b/build/cjs/nodes/callout/callout-parser.js
@@ -5,9 +5,36 @@ const getColorTag = (nodeElem) => {
     const colorClass = nodeElem.classList?.value?.match(/kg-callout-card-(\w+)/);
     return colorClass && colorClass[1];
 };
+const getDataCalloutTitle = (titleNode) => {
+    if (!titleNode) {
+        return '';
+    }
+    const clone = titleNode.cloneNode(true);
+    clone.querySelector('[data-callout-icon]')?.remove();
+    return clone.textContent?.trim() || '';
+};
 function parseCalloutNode(CalloutNode) {
     return {
         div: (nodeElem) => {
+            const isHomeCallout = nodeElem.hasAttribute('data-callout');
+            if (nodeElem.tagName === 'DIV' && isHomeCallout) {
+                return {
+                    conversion(domNode) {
+                        const titleNode = domNode?.querySelector('[data-callout-title]');
+                        const bodyNode = domNode?.querySelector('[data-callout-body]');
+                        const iconNode = domNode?.querySelector('[data-callout-icon]');
+                        const payload = {
+                            calloutText: bodyNode && bodyNode.innerHTML.trim() || '',
+                            calloutTitle: getDataCalloutTitle(titleNode),
+                            calloutIcon: iconNode?.getAttribute('data-callout-icon') || '',
+                            backgroundColor: domNode?.getAttribute('data-callout-type') || 'info'
+                        };
+                        const node = new CalloutNode(payload);
+                        return { node };
+                    },
+                    priority: 1
+                };
+            }
             const isKgCalloutCard = nodeElem.classList?.contains('kg-callout-card');
             if (nodeElem.tagName === 'DIV' && isKgCalloutCard) {
                 return {
@@ -30,3 +57,4 @@ function parseCalloutNode(CalloutNode) {
         }
     };
 }
+//# sourceMappingURL=callout-parser.js.map
diff --git a/build/cjs/nodes/callout/callout-renderer.js b/build/cjs/nodes/callout/callout-renderer.js
index 3eb01d476048ab5d1de12e44badeffaab929a092..552ddd9bbc3cb4d69dbe3cb9486476408b430ad5 100644
--- a/build/cjs/nodes/callout/callout-renderer.js
+++ b/build/cjs/nodes/callout/callout-renderer.js
@@ -3,25 +3,86 @@ Object.defineProperty(exports, "__esModule", { value: true });
 exports.renderCalloutNode = renderCalloutNode;
 const add_create_document_option_js_1 = require("../../utils/add-create-document-option.js");
 const clean_dom_js_1 = require("../../utils/clean-dom.js");
+const calloutTypeLabels = {
+    note: 'Note',
+    info: 'Info',
+    tip: 'Tip',
+    hint: 'Hint',
+    important: 'Important',
+    warning: 'Warning',
+    caution: 'Caution',
+    attention: 'Attention',
+    danger: 'Danger',
+    error: 'Error',
+    bug: 'Bug',
+    quote: 'Quote',
+    cite: 'Citation'
+};
+const legacyColorToType = {
+    white: 'note',
+    grey: 'note',
+    blue: 'info',
+    green: 'tip',
+    yellow: 'warning',
+    red: 'danger',
+    pink: 'danger',
+    purple: 'important',
+    accent: 'important'
+};
+const typeDefaultIcons = {
+    note: 'info',
+    info: 'info',
+    tip: 'lightbulb',
+    hint: 'lightbulb',
+    important: 'sparkles',
+    warning: 'triangle-alert',
+    caution: 'triangle-alert',
+    attention: 'triangle-alert',
+    danger: 'circle-x',
+    error: 'circle-x',
+    bug: 'circle-x',
+    quote: 'message-square-quote',
+    cite: 'message-square-quote'
+};
+const lucideIcons = {
+    info: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-info" aria-hidden="true"><circle cx="12" cy="12" r="10"></circle><path d="M12 16v-4"></path><path d="M12 8h.01"></path></svg>',
+    lightbulb: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-lightbulb" aria-hidden="true"><path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"></path><path d="M9 18h6"></path><path d="M10 22h4"></path></svg>',
+    sparkles: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-sparkles" aria-hidden="true"><path d="M11.017 2.814a1 1 0 0 1 1.966 0l1.051 5.558a2 2 0 0 0 1.594 1.594l5.558 1.051a1 1 0 0 1 0 1.966l-5.558 1.051a2 2 0 0 0-1.594 1.594l-1.051 5.558a1 1 0 0 1-1.966 0l-1.051-5.558a2 2 0 0 0-1.594-1.594l-5.558-1.051a1 1 0 0 1 0-1.966l5.558-1.051a2 2 0 0 0 1.594-1.594z"></path><path d="M20 2v4"></path><path d="M22 4h-4"></path><circle cx="4" cy="20" r="2"></circle></svg>',
+    'triangle-alert': '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-triangle-alert" aria-hidden="true"><path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3"></path><path d="M12 9v4"></path><path d="M12 17h.01"></path></svg>',
+    'circle-x': '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-circle-x" aria-hidden="true"><circle cx="12" cy="12" r="10"></circle><path d="m15 9-6 6"></path><path d="m9 9 6 6"></path></svg>',
+    'message-square-quote': '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-message-square-quote" aria-hidden="true"><path d="M14 14a2 2 0 0 0 2-2V8h-2"></path><path d="M22 17a2 2 0 0 1-2 2H6.828a2 2 0 0 0-1.414.586l-2.202 2.202A.71.71 0 0 1 2 21.286V5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2z"></path><path d="M8 14a2 2 0 0 0 2-2V8H8"></path></svg>'
+};
+function normalizeCalloutType(type) {
+    const normalizedType = (type || '').toLowerCase();
+    return calloutTypeLabels[normalizedType] ? normalizedType : legacyColorToType[normalizedType] || 'info';
+}
+function getCalloutIconName(node, type) {
+    if (node.calloutIcon && lucideIcons[node.calloutIcon]) {
+        return node.calloutIcon;
+    }
+    return typeDefaultIcons[type] || 'info';
+}
 function renderCalloutNode(node, options = {}) {
     (0, add_create_document_option_js_1.addCreateDocumentOption)(options);
     const document = options.createDocument();
+    const type = normalizeCalloutType(node.backgroundColor);
+    const title = node.calloutTitle || calloutTypeLabels[type] || 'Info';
+    const iconName = getCalloutIconName(node, type);
     const element = document.createElement('div');
-    // backgroundColor can end up with `rgba(0, 0, 0, 0)` from old mobiledoc copy/paste
-    // that is invalid when used in a class name so fall back to `white` when we don't have
-    // something that looks like a valid class
-    if (!node.backgroundColor || !node.backgroundColor.match(/^[a-zA-Z\d-]+$/)) {
-        node.backgroundColor = 'white';
-    }
-    element.classList.add('kg-card', 'kg-callout-card', `kg-callout-card-${node.backgroundColor}`);
-    if (node.calloutEmoji) {
-        const emojiElement = document.createElement('div');
-        emojiElement.classList.add('kg-callout-emoji');
-        emojiElement.textContent = node.calloutEmoji;
-        element.appendChild(emojiElement);
+    element.setAttribute('data-callout', '');
+    element.setAttribute('data-callout-type', type);
+    const titleElement = document.createElement('div');
+    titleElement.setAttribute('data-callout-title', '');
+    if (iconName && lucideIcons[iconName]) {
+        const iconElement = document.createElement('span');
+        iconElement.setAttribute('data-callout-icon', iconName);
+        iconElement.innerHTML = lucideIcons[iconName];
+        titleElement.appendChild(iconElement);
     }
+    titleElement.appendChild(document.createTextNode(title));
+    element.appendChild(titleElement);
     const textElement = document.createElement('div');
-    textElement.classList.add('kg-callout-text');
+    textElement.setAttribute('data-callout-body', '');
     const temporaryContainer = document.createElement('div');
     temporaryContainer.innerHTML = node.calloutText;
     const allowedTags = ['A', 'STRONG', 'EM', 'B', 'I', 'BR', 'CODE', 'MARK', 'S', 'DEL', 'U', 'SUP', 'SUB'];
@@ -30,3 +91,4 @@ function renderCalloutNode(node, options = {}) {
     element.appendChild(textElement);
     return { element, type: 'outer' };
 }
+//# sourceMappingURL=callout-renderer.js.map
diff --git a/build/esm/nodes/callout/CalloutNode.d.ts b/build/esm/nodes/callout/CalloutNode.d.ts
index 7d23e26a8cfdaaaf82fa44199ee8cedd5026840a..e9a2ffbe9606dbdd0262e0e63be29570afae2e78 100644
--- a/build/esm/nodes/callout/CalloutNode.d.ts
+++ b/build/esm/nodes/callout/CalloutNode.d.ts
@@ -1,15 +1,21 @@
 export interface CalloutData {
     calloutText?: string;
+    calloutTitle?: string;
+    calloutIcon?: string;
     calloutEmoji?: string;
     backgroundColor?: string;
 }
 export interface CalloutNode {
     calloutText: string;
+    calloutTitle: string;
+    calloutIcon: string;
     calloutEmoji: string;
     backgroundColor: string;
 }
 declare const CalloutNode_base: import("../../generate-decorator-node.js").GeneratedDecoratorNodeClass<{
     calloutText: string;
+    calloutTitle: string;
+    calloutIcon: string;
     calloutEmoji: string;
     backgroundColor: string;
 }, {
@@ -17,7 +23,7 @@ declare const CalloutNode_base: import("../../generate-decorator-node.js").Gener
     type: "outer";
 }>;
 export declare class CalloutNode extends CalloutNode_base {
-    constructor({ calloutText, calloutEmoji, backgroundColor }?: CalloutData, key?: string);
+    constructor({ calloutText, calloutTitle, calloutIcon, calloutEmoji, backgroundColor }?: CalloutData, key?: string);
     static importDOM(): {
         div: (nodeElem: HTMLElement) => {
             conversion(domNode: HTMLElement): {
diff --git a/build/esm/nodes/callout/CalloutNode.js b/build/esm/nodes/callout/CalloutNode.js
index f622b325fa040ed913033c115bb066bc796d086f..7778efd245f84d7f3be54260be5602b1fdc5640d 100644
--- a/build/esm/nodes/callout/CalloutNode.js
+++ b/build/esm/nodes/callout/CalloutNode.js
@@ -1,10 +1,43 @@
 import { generateDecoratorNode } from '../../generate-decorator-node.js';
 import { renderCalloutNode } from './callout-renderer.js';
 import { parseCalloutNode } from './callout-parser.js';
+const legacyColorToType = {
+    white: 'note',
+    grey: 'note',
+    blue: 'info',
+    green: 'tip',
+    yellow: 'warning',
+    red: 'danger',
+    pink: 'danger',
+    purple: 'important',
+    accent: 'important'
+};
+const typeDefaultIcons = {
+    note: 'info',
+    info: 'info',
+    tip: 'lightbulb',
+    hint: 'lightbulb',
+    important: 'sparkles',
+    warning: 'triangle-alert',
+    caution: 'triangle-alert',
+    attention: 'triangle-alert',
+    danger: 'circle-x',
+    error: 'circle-x',
+    bug: 'circle-x',
+    quote: 'message-square-quote',
+    cite: 'message-square-quote'
+};
+const validTypes = new Set([...Object.keys(typeDefaultIcons)]);
+function normalizeCalloutType(type) {
+    const normalizedType = (type || '').toLowerCase();
+    return validTypes.has(normalizedType) ? normalizedType : legacyColorToType[normalizedType] || 'info';
+}
 const calloutProperties = [
     { name: 'calloutText', default: '', wordCount: true },
-    { name: 'calloutEmoji', default: '💡' },
-    { name: 'backgroundColor', default: 'blue' }
+    { name: 'calloutTitle', default: '' },
+    { name: 'calloutIcon', default: 'info' },
+    { name: 'calloutEmoji', default: '' },
+    { name: 'backgroundColor', default: 'info' }
 ];
 export class CalloutNode extends generateDecoratorNode({
     nodeType: 'callout',
@@ -12,11 +45,14 @@ export class CalloutNode extends generateDecoratorNode({
     defaultRenderFn: renderCalloutNode
 }) {
     /* override */
-    constructor({ calloutText, calloutEmoji, backgroundColor } = {}, key) {
+    constructor({ calloutText, calloutTitle, calloutIcon, calloutEmoji, backgroundColor } = {}, key) {
         super({}, key);
+        const type = normalizeCalloutType(backgroundColor);
         this.__calloutText = calloutText || '';
-        this.__calloutEmoji = calloutEmoji ?? '💡';
-        this.__backgroundColor = backgroundColor || 'blue';
+        this.__calloutTitle = calloutTitle || '';
+        this.__calloutIcon = calloutIcon || typeDefaultIcons[type] || 'info';
+        this.__calloutEmoji = calloutEmoji || '';
+        this.__backgroundColor = type;
     }
     static importDOM() {
         return parseCalloutNode(this);
diff --git a/build/esm/nodes/callout/callout-parser.js b/build/esm/nodes/callout/callout-parser.js
index 9ee1da16397aec722e99d3bf0f79434aae45d36e..bd7dd1dae08bf12cc37fd8ee4398ba3d3ddbc2c3 100644
--- a/build/esm/nodes/callout/callout-parser.js
+++ b/build/esm/nodes/callout/callout-parser.js
@@ -2,9 +2,36 @@ const getColorTag = (nodeElem) => {
     const colorClass = nodeElem.classList?.value?.match(/kg-callout-card-(\w+)/);
     return colorClass && colorClass[1];
 };
+const getDataCalloutTitle = (titleNode) => {
+    if (!titleNode) {
+        return '';
+    }
+    const clone = titleNode.cloneNode(true);
+    clone.querySelector('[data-callout-icon]')?.remove();
+    return clone.textContent?.trim() || '';
+};
 export function parseCalloutNode(CalloutNode) {
     return {
         div: (nodeElem) => {
+            const isHomeCallout = nodeElem.hasAttribute('data-callout');
+            if (nodeElem.tagName === 'DIV' && isHomeCallout) {
+                return {
+                    conversion(domNode) {
+                        const titleNode = domNode?.querySelector('[data-callout-title]');
+                        const bodyNode = domNode?.querySelector('[data-callout-body]');
+                        const iconNode = domNode?.querySelector('[data-callout-icon]');
+                        const payload = {
+                            calloutText: bodyNode && bodyNode.innerHTML.trim() || '',
+                            calloutTitle: getDataCalloutTitle(titleNode),
+                            calloutIcon: iconNode?.getAttribute('data-callout-icon') || '',
+                            backgroundColor: domNode?.getAttribute('data-callout-type') || 'info'
+                        };
+                        const node = new CalloutNode(payload);
+                        return { node };
+                    },
+                    priority: 1
+                };
+            }
             const isKgCalloutCard = nodeElem.classList?.contains('kg-callout-card');
             if (nodeElem.tagName === 'DIV' && isKgCalloutCard) {
                 return {
diff --git a/build/esm/nodes/callout/callout-renderer.d.ts b/build/esm/nodes/callout/callout-renderer.d.ts
index 95e4756db0f32340d4a45a547585c45374e6dfaa..e50324b66a2c9a9eae6ab3b5de3c73ed95e73213 100644
--- a/build/esm/nodes/callout/callout-renderer.d.ts
+++ b/build/esm/nodes/callout/callout-renderer.d.ts
@@ -2,7 +2,9 @@ import type { ExportDOMOptions } from '../../export-dom.js';
 interface CalloutNodeData {
     backgroundColor: string;
     calloutEmoji: string;
+    calloutIcon: string;
     calloutText: string;
+    calloutTitle: string;
 }
 interface RenderOptions extends ExportDOMOptions {
 }
diff --git a/build/esm/nodes/callout/callout-renderer.js b/build/esm/nodes/callout/callout-renderer.js
index 74eae232f6f205d162946c0bfde817bcc9f775c8..eccc2deb2f24538b42a0009b46151ad26e8db06c 100644
--- a/build/esm/nodes/callout/callout-renderer.js
+++ b/build/esm/nodes/callout/callout-renderer.js
@@ -1,24 +1,85 @@
 import { addCreateDocumentOption } from '../../utils/add-create-document-option.js';
 import { cleanDOM } from '../../utils/clean-dom.js';
+const calloutTypeLabels = {
+    note: 'Note',
+    info: 'Info',
+    tip: 'Tip',
+    hint: 'Hint',
+    important: 'Important',
+    warning: 'Warning',
+    caution: 'Caution',
+    attention: 'Attention',
+    danger: 'Danger',
+    error: 'Error',
+    bug: 'Bug',
+    quote: 'Quote',
+    cite: 'Citation'
+};
+const legacyColorToType = {
+    white: 'note',
+    grey: 'note',
+    blue: 'info',
+    green: 'tip',
+    yellow: 'warning',
+    red: 'danger',
+    pink: 'danger',
+    purple: 'important',
+    accent: 'important'
+};
+const typeDefaultIcons = {
+    note: 'info',
+    info: 'info',
+    tip: 'lightbulb',
+    hint: 'lightbulb',
+    important: 'sparkles',
+    warning: 'triangle-alert',
+    caution: 'triangle-alert',
+    attention: 'triangle-alert',
+    danger: 'circle-x',
+    error: 'circle-x',
+    bug: 'circle-x',
+    quote: 'message-square-quote',
+    cite: 'message-square-quote'
+};
+const lucideIcons = {
+    info: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-info" aria-hidden="true"><circle cx="12" cy="12" r="10"></circle><path d="M12 16v-4"></path><path d="M12 8h.01"></path></svg>',
+    lightbulb: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-lightbulb" aria-hidden="true"><path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"></path><path d="M9 18h6"></path><path d="M10 22h4"></path></svg>',
+    sparkles: '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-sparkles" aria-hidden="true"><path d="M11.017 2.814a1 1 0 0 1 1.966 0l1.051 5.558a2 2 0 0 0 1.594 1.594l5.558 1.051a1 1 0 0 1 0 1.966l-5.558 1.051a2 2 0 0 0-1.594 1.594l-1.051 5.558a1 1 0 0 1-1.966 0l-1.051-5.558a2 2 0 0 0-1.594-1.594l-5.558-1.051a1 1 0 0 1 0-1.966l5.558-1.051a2 2 0 0 0 1.594-1.594z"></path><path d="M20 2v4"></path><path d="M22 4h-4"></path><circle cx="4" cy="20" r="2"></circle></svg>',
+    'triangle-alert': '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-triangle-alert" aria-hidden="true"><path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3"></path><path d="M12 9v4"></path><path d="M12 17h.01"></path></svg>',
+    'circle-x': '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-circle-x" aria-hidden="true"><circle cx="12" cy="12" r="10"></circle><path d="m15 9-6 6"></path><path d="m9 9 6 6"></path></svg>',
+    'message-square-quote': '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-message-square-quote" aria-hidden="true"><path d="M14 14a2 2 0 0 0 2-2V8h-2"></path><path d="M22 17a2 2 0 0 1-2 2H6.828a2 2 0 0 0-1.414.586l-2.202 2.202A.71.71 0 0 1 2 21.286V5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2z"></path><path d="M8 14a2 2 0 0 0 2-2V8H8"></path></svg>'
+};
+function normalizeCalloutType(type) {
+    const normalizedType = (type || '').toLowerCase();
+    return calloutTypeLabels[normalizedType] ? normalizedType : legacyColorToType[normalizedType] || 'info';
+}
+function getCalloutIconName(node, type) {
+    if (node.calloutIcon && lucideIcons[node.calloutIcon]) {
+        return node.calloutIcon;
+    }
+    return typeDefaultIcons[type] || 'info';
+}
 export function renderCalloutNode(node, options = {}) {
     addCreateDocumentOption(options);
     const document = options.createDocument();
+    const type = normalizeCalloutType(node.backgroundColor);
+    const title = node.calloutTitle || calloutTypeLabels[type] || 'Info';
+    const iconName = getCalloutIconName(node, type);
     const element = document.createElement('div');
-    // backgroundColor can end up with `rgba(0, 0, 0, 0)` from old mobiledoc copy/paste
-    // that is invalid when used in a class name so fall back to `white` when we don't have
-    // something that looks like a valid class
-    if (!node.backgroundColor || !node.backgroundColor.match(/^[a-zA-Z\d-]+$/)) {
-        node.backgroundColor = 'white';
-    }
-    element.classList.add('kg-card', 'kg-callout-card', `kg-callout-card-${node.backgroundColor}`);
-    if (node.calloutEmoji) {
-        const emojiElement = document.createElement('div');
-        emojiElement.classList.add('kg-callout-emoji');
-        emojiElement.textContent = node.calloutEmoji;
-        element.appendChild(emojiElement);
+    element.setAttribute('data-callout', '');
+    element.setAttribute('data-callout-type', type);
+    const titleElement = document.createElement('div');
+    titleElement.setAttribute('data-callout-title', '');
+    if (iconName && lucideIcons[iconName]) {
+        const iconElement = document.createElement('span');
+        iconElement.setAttribute('data-callout-icon', iconName);
+        iconElement.innerHTML = lucideIcons[iconName];
+        titleElement.appendChild(iconElement);
     }
+    titleElement.appendChild(document.createTextNode(title));
+    element.appendChild(titleElement);
     const textElement = document.createElement('div');
-    textElement.classList.add('kg-callout-text');
+    textElement.setAttribute('data-callout-body', '');
     const temporaryContainer = document.createElement('div');
     temporaryContainer.innerHTML = node.calloutText;
     const allowedTags = ['A', 'STRONG', 'EM', 'B', 'I', 'BR', 'CODE', 'MARK', 'S', 'DEL', 'U', 'SUP', 'SUB'];
